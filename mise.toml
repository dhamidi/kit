[tools]
ast-grep = "latest"
bun = "1.3.14"

[tasks.release]
description = "Build cross-platform kit binaries and upload them to a GitHub release"
usage = "arg <version>"
run = "bun scripts/release.js $usage_version"
